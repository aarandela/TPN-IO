import React, { Component } from 'react'
import DateRange from '../../DateRange'

export default class Urine extends Component {
  render () {
    return (
      <div>
        <DateRange />
      </div>

    )
  }
}
